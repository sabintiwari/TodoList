<div class="page-header">
  <%= link_to list_items_id_path(params[:id]), :class => 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1>New List Item</h1>
</div>

<%= render partial: 'form' , locals: {list_id: params[:id]} %>
